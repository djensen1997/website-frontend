import { Home, Book } from '@material-ui/icons';

// holds the info for all the pages in the app
const pageInfo =  [
  {
    name: 'Home',
    icon: Home,
    route: '/',
  },
  {
    name: 'About Me',
    icon: Book,
    route: '/About'
  }
]

export default pageInfo